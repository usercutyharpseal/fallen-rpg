# v0.9.26 배포

Termux에서 기존 `~/textrpg` v0.9.25에 패치 파일을 적용합니다.

```bash
bash ~/storage/downloads/fallen_patch_v0926.sh
```

적용 후 Git 배포:

```bash
cd ~/textrpg
git add .
git commit -m "Lord Algon detail + unified ranking v0.9.26"
git push
```

확인 포인트:
- 메인 화면 `THE FALLEN · v0.9.26`
- HARD SOLO 시작 골드 30
- 하드 프롤로그 직후 북부 경계 장터
- 최후의 기사 / 데몬 알터닐은 프리뷰 유지
- 통합 랭킹에 NORMAL/HARD 기록이 함께 표시


## v0.9.30
- 메뉴 하단 4자리 기기 데이터 로드 키 추가
- 키는 10분 유효, 1회 사용, 생성/오입력 시도 제한
- 진행 세이브/메타/랭킹 ID/PVP 프로필 이전
- 직업별 영구 성장: 노말 정상엔딩 3회당 전 능력치 +1(노말 최대 +2)
- 하드 정상엔딩 1회당 전 능력치 +1, 직업 최종 영구 보너스 최대 +3
- BAD END 및 bad:true 엔딩은 직업 성장 카운트에서 완전 제외


## v0.9.31 · 노말 히든 엔딩 / 엔딩 점검
- 박쥐: 두 개의 깃발보다 더 많이 진영을 변경(왕국↔도적단 2회 이상)한 뒤 정상 진영 엔딩 도달. 보너스 19,000.
- 신사: 처치 0, 왕 에드란 또는 세리아에게 처세/도망 성공, 모두와 친구 조건 미충족. 보너스 4,500.
- 억제자: 처치 0, 최종 대치에서 무혈 해결, 현재 공격력 18 이상. 신사보다 우선하며 보너스 36,000.
- 억제자 > 박쥐 > 두 개의 깃발/신사 순으로 충돌 우선순위 정리.
- 반란 엔딩 표기를 노말 모드에 맞춰 NORMAL END · 반란으로 수정.
- 팬텀/끝없는 악몽은 BAD END 유지(직업 성장 카운트 제외).
- 기존 7개 배드엔딩의 클라이언트/서버 점수 보너스 누락을 동기화.


## v0.9.32 · 팬텀 생환 히든엔딩
- 팬텀 조우에서 도망 판정 성공 시 phantomEscaped 플래그 저장.
- 이후 정상 결말에 도달하면 원래 결말 대신 '팬텀' 히든엔딩 출력.
- 팬텀 히든엔딩 보너스 30,000.
- 팬텀에게 직접 사망하는 기존 배드엔딩은 '팬텀에게 삼켜진 자'로 분리, 1,800 유지.
- '끝없는 악몽' 등 배드엔딩으로 끝난 경우 팬텀 히든엔딩으로 덮어쓰지 않음.


## v0.9.33
- 하드 로드 알곤 루트 도망 성공 처리 수정.
- 도망 성공 시 ESCAPE_ROUTES의 실제 다음 장면으로 이동.
- 하드 도망 가능 조우 9곳 경로 검증 추가.
- 로드 알곤 최종전 도망 불가 기믹 유지.


## v0.9.34 · 신규 직업
- 언데드(남성 초상): 체력 10 / 공격 6 / 처세 6 / 속도 3, 기본 해금.
- 죽지 못한 자: 공격/처세/도망 실패 직전에 1회 부활하여 같은 조우를 처음부터 다시 시도. 적 4킬마다 사용 횟수 1회로 재충전.
- 대부(여성 초상): 체력 8 / 공격 2 / 처세 8 / 속도 2.
- 너라면 함께할 가치가 있겠군: 처세 성공마다 공격력 +4.
- 대부 해금: 노말 정상엔딩 7회 또는 하드 정상엔딩 1회.
- 이전 버전의 직업별 하드 정상엔딩 성장 기록이 있으면 하드 클리어 수를 역산해 대부 해금에 반영.
- 독재자: 독재 발동 요구치 10 → 8.
- 신규 초상은 이미 생성된 남성 언데드/여성 대부 이미지만 사용. 추가 이미지 생성 없음.


## v0.9.35
- 언데드를 잠금 직업들 뒤에서 기본 해금 직업군 위쪽으로 이동.
- 직업 선택 순서: 기사 → 귀족 → 도둑 → 상인 → 언데드 → 잠금 직업군.
- 언데드 기본 공격 6 → 7, 처세 6 → 7.
- 최종 기본 능력치: 체력 10 / 공격 7 / 처세 7 / 속도 3.


## v0.9.36
- 직업 선택 카드의 성장 진행 각주 제거.
- `성장 +1/+2/+3` 태그는 유지.
- 패시브 설명 뒤의 `성장 +N · 노말 x/3 · 하드 x/3` 문구만 숨김.
- 실제 영구 능력치 성장과 카운트는 그대로 유지.


## v0.9.37
- 직업 패시브 설명에서 해금 조건 문구 제거.
- 대부 설명은 `처세에 성공할 때마다 공격력이 4 오른다.`만 표시.
- 해금 조건은 잠금 상태 UI에서만 표시.
