# v0.9.21 배포

```bash
bash ~/storage/downloads/fallen_patch_v0921.sh
cd ~/textrpg
git add .
git commit -m "Illustrations and demon pact v0.9.21"
git push
```

Render Auto-Deploy가 On Commit이면 push 후 자동 배포된다.
메인 화면에 `THE FALLEN · v0.9.21`이 표시되는지 확인한다.
